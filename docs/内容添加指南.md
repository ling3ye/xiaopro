# 内容添加指南

本文档详细说明了如何添加各类内容到项目中，包括文章、开发板、模块、实验、方案等。

---

## 目录

1. [系统概述](#系统概述)
2. [开发板 (Boards)](#开发板-boards)
3. [外设模块 (Modules)](#外设模块-modules)
4. [模块文档 (ModuleDocs)](#模块文档-moduledocs)
5. [基础实验 (Experiments)](#基础实验-experiments)
6. [实战方案 (Solutions)](#实战方案-solutions)
7. [博客文章 (Articles)](#博客文章-articles)
8. [多语言支持](#多语言支持)
9. [关联关系说明](#关联关系说明)
10. [常见问题](#常见问题)

---

## 系统概述

本项目使用 **Astro Content** 集合系统进行内容管理，共包含 6 种内容类型：

| 类型 | 用途 | 文件格式 | 存储位置 |
|------|------|----------|----------|
| **boards** | 开发板/主控板信息 | JSON | `src/content/boards/` |
| **modules** | 外设模块信息 | JSON | `src/content/modules/` |
| **moduleDocs** | 模块详细说明文档 | Markdown | `src/content/moduleDocs/` |
| **experiments** | 基础实验 (单对单) | Markdown | `src/content/experiments/` |
| **solutions** | 实战方案 (一对多) | Markdown | `src/content/solutions/` |
| **articles** | 博客文章 | Markdown | `src/content/articles/` |

---

## 开发板 (Boards)

### 用途
存储各类开发板、主控板的基本信息和技术规格。

### 文件位置
```
src/content/boards/
├── esp32-c3.json
├── esp32-s3.json
├── arduino-uno.json
├── rp2040.json
└── ...
```

### 文件命名规范
- 使用小写字母和连字符
- 格式：`品牌-型号.json`
- 示例：`esp32-s3.json`、`arduino-uno.json`

### 属性字段

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `brand` | string | ✅ | 品牌/厂商名称，如 `Espressif`、`Arduino` |
| `model` | string | ✅ | 型号名称，如 `ESP32-S3-DevKitC-1` |
| `specs` | object | ❌ | 硬件参数字典，键值对形式 |
| `officialUrl` | string(url) | ❌ | 官方文档链接 |
| `keywords` | array | ❌ | 关键词数组，用于搜索优化 |
| `image` | string | ❌ | 缩略图路径（相对于 public 目录） |

### `specs` 参数说明
`specs` 是一个灵活的字典对象，可以包含任意参数：

| 键名示例 | 值类型 | 说明 |
|----------|--------|------|
| `cpu` | string | CPU 型号，如 `"Xtensa LX7 Dual-core"` |
| `frequency` | string | 主频，如 `"240MHz"` |
| `flash` | string | Flash 大小，如 `"8MB"` |
| `psram` | string | PSRAM 大小，如 `"8MB"` |
| `wifi` | boolean | 是否支持 WiFi |
| `bluetooth` | string | 蓝牙版本，如 `"5.0"` |

### 完整示例

```json
{
  "brand": "Espressif",
  "model": "ESP32-S3-DevKitC-1",
  "specs": {
    "cpu": "Xtensa LX7 Dual-core",
    "frequency": "240MHz",
    "flash": "8MB",
    "psram": "8MB",
    "wifi": true,
    "bluetooth": "5.0"
  },
  "keywords": ["dual-core", "ai", "usb-otg", "high-performance"],
  "officialUrl": "https://docs.espressif.com/projects/esp-idf/en/latest/esp32s3/"
}
```

### 添加步骤

1. 在 `src/content/boards/` 目录下创建新的 JSON 文件
2. 文件名使用小写字母和连字符，如 `new-board-name.json`
3. 填写必需字段：`brand` 和 `model`
4. 根据需要添加可选字段
5. 保存文件，系统会自动识别

---

## 外设模块 (Modules)

### 用途
存储各类外设模块的基本信息，如传感器、显示器、执行器等。

### 文件位置
```
src/content/modules/
├── sensor/
│   ├── dht11.json
│   ├── bme280.json
│   └── ...
├── display/
│   ├── ssd1306.json
│   ├── st7735.json
│   └── ...
├── actuator/
│   ├── servo-sg90.json
│   └── ...
├── communication/
│   ├── nrf24l01.json
│   └── ...
├── power/
│   └── ...
└── other/
    └── ...
```

### 文件命名规范
- 按分类存储在子目录中
- 使用小写字母和连字符
- 示例：`sensor/dht11.json`、`display/ssd1306.json`

### 属性字段

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `category` | enum | ✅ | 模块分类 |
| `model` | string | ✅ | 型号名称 |
| `brand` | string | ❌ | 品牌/厂商名称 |
| `alias` | array | ❌ | 别名数组，支持多语言 |
| `specs` | object | ❌ | 技术参数字典 |
| `officialUrl` | string(url) | ❌ | 官方文档链接 |
| `datasheet` | string(url) | ❌ | 数据手册链接 |

### `category` 分类枚举值

| 值 | 说明 |
|----|------|
| `sensor` | 传感器类（温度、湿度、压力等） |
| `display` | 显示器类（OLED、LCD、TFT 等） |
| `actuator` | 执行器类（电机、继电器、舵机等） |
| `communication` | 通信模块（WiFi、蓝牙、LoRa 等） |
| `power` | 电源模块（电池、充电管理器等） |
| `other` | 其他类别 |

### 完整示例

```json
{
  "category": "sensor",
  "brand": "Lingshun",
  "model": "DHT11",
  "alias": ["温湿度传感器", "Temperature & Humidity Sensor"],
  "specs": {
    "voltage": "3.3V - 5V",
    "interface": "1-Wire",
    "rangeTemp": "0-50°C"
  },
  "officialUrl": "https://www.example.com/dht11",
  "datasheet": "https://www.example.com/dht11-datasheet.pdf"
}
```

### 添加步骤

1. 确定模块分类，在 `src/content/modules/` 对应子目录下创建文件
2. 填写必需字段：`category` 和 `model`
3. 根据需要添加可选字段
4. 保存文件

---

## 模块文档 (ModuleDocs)

### 用途
为特定模块提供详细的使用说明、技术规格、应用场景等信息。

### 文件位置
```
src/content/moduleDocs/
├── zh-cn/
│   └── display-ssd1306.md
└── en/
    └── display-ssd1306.md
```

### 文件命名规范
- 按语言分目录存储
- 文件名建议格式：`分类-型号.md`
- 示例：`display-ssd1306.md`、`sensor-dht11.md`

### Front Matter 属性

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `moduleId` | reference | ✅ | 关联的模块 ID，格式为 `分类/文件名` |

### `moduleId` 格式说明
`moduleId` 必须与 `modules` 集合中的文件路径对应：
- 格式：`category/filename`
- 示例：`display/ssd1306`、`sensor/dht11`

### 完整示例

```markdown
---
moduleId: display/ssd1306
---

# SSD1306 OLED 显示模块

SSD1306 是一款常用的 OLED 显示驱动芯片，广泛应用于各类小型显示模块。

## 主要特性

- **超低功耗**：正常工作电流仅为几微安
- **高对比度**：自发光技术，对比度远超传统 LCD
- **宽视角**：接近 180 度的视角范围

## 技术规格

| 参数 | 值 |
|------|-----|
| 驱动芯片 | SSD1306 |
| 显示颜色 | 单色（蓝色/白/黄） |
| 工作电压 | 3.3V - 5V |

## 使用建议

1. **上电顺序**：确保 VCC 稳定后再连接 I2C/SPI 信号
2. **初始化**：每次上电后需要进行完整的初始化序列

## 常见问题

**Q: 为什么屏幕是全黑的？**
A: 检查初始化序列是否完整，特别是显示开启指令。
```

### 添加步骤

1. 确认目标模块已在 `modules` 集合中定义
2. 在 `src/content/moduleDocs/zh-cn/` 或 `en/` 目录下创建 Markdown 文件
3. 在 Front Matter 中设置正确的 `moduleId`
4. 撰写详细的文档内容
5. 保存文件

---

## 基础实验 (Experiments)

### 用途
记录基础的单对单实验，一个开发板 + 一个模块的实验记录。

### 文件位置
```
src/content/experiments/
├── esp32/
│   └── zh-cn/
│       ├── esp32-dht11.md
│       ├── esp32-servo.md
│       └── ...
├── arduino/
│   └── zh-cn/
│       └── arduino-ssd1306.md
├── rp/
│   └── zh-cn/
│       └── rp2040-bme280.md
├── stm32/
│   └── zh-cn/
│       └── stm32-blink.md
└── other/
    └── zh-cn/
        └── nrf52840-bme680.md
```

### 文件命名规范
- 按开发板分类存储（`category` 字段）
- 按语言分目录存储
- 文件名建议：`开发板名-模块名.md`
- 示例：`esp32-dht11.md`、`rp2040-bme280.md`

### Front Matter 属性

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `title` | string | ✅ | 实验标题 |
| `boardId` | reference | ✅ | 关联的开发板 ID（boards 文件名） |
| `moduleId` | reference | ✅ | 关联的模块 ID（`分类/文件名`） |
| `category` | enum | ❌ | 开发板分类 |
| `date` | date | ❌ | 实验日期 |
| `intro` | string | ❌ | 实验简介 |
| `image` | string | ❌ | 缩略图路径 |

### `category` 分类枚举值

| 值 | 说明 |
|----|------|
| `esp32` | ESP32 系列开发板 |
| `arduino` | Arduino 系列开发板 |
| `rp` | Raspberry Pi Pico / RP2040 系列 |
| `stm32` | STM32 系列开发板 |
| `other` | 其他开发板 |

### `boardId` 格式
直接使用 boards 文件名（不含扩展名）：
- 示例：`esp32-c3`、`esp32-s3`、`arduino-uno`

### `moduleId` 格式
格式为 `分类/文件名`：
- 示例：`sensor/dht11`、`display/ssd1306`、`actuator/servo-sg90`

### 完整示例

```markdown
---
title: "ESP32 读取 DHT11 温湿度"
boardId: "esp32-c3"
moduleId: "sensor/dht11"
category: "esp32"
date: 2023-10-01
intro: "最经典的入门实验，学会使用单总线协议读取环境数据。"
image: "https://picsum.photos/400/200"
---

## 实验步骤

1. 将 DHT11 的 VCC 接到 ESP32 的 3.3V
2. 将 DATA 接到 GPIO 4
3. 烧录代码...

## 代码示例

```cpp
// DHT11 读取代码
#include <DHT.h>
#define DHTPIN 4
#define DHTTYPE DHT11

DHT dht(DHTPIN, DHTTYPE);

void setup() {
  Serial.begin(115200);
  dht.begin();
}

void loop() {
  float h = dht.readHumidity();
  float t = dht.readTemperature();
  Serial.print("Humidity: ");
  Serial.print(h);
  Serial.print("%  Temperature: ");
  Serial.print(t);
  Serial.println("°C");
  delay(2000);
}
```

## 接线图

| DHT11 引脚 | ESP32 引脚 |
|-----------|-----------|
| VCC | 3.3V |
| DATA | GPIO 4 |
| GND | GND |
```

### 添加步骤

1. 确认目标开发板和模块已在对应集合中定义
2. 确定开发板分类（`category`），选择对应目录
3. 在 `src/content/experiments/[category]/zh-cn/` 或 `en/` 创建文件
4. 填写 Front Matter，确保 `boardId` 和 `moduleId` 正确
5. 撰写实验内容
6. 保存文件

---

## 实战方案 (Solutions)

### 用途
记录实战项目方案，一个开发板 + 多个模块的完整解决方案。

### 文件位置
```
src/content/solutions/
├── zh-cn/
│   ├── smart-garden.md
│   ├── weather-station.md
│   ├── smart-plant-monitor.md
│   └── ...
└── en/
    └── ...
```

### 文件命名规范
- 按语言分目录存储
- 使用小写字母和连字符
- 建议格式：`项目名称.md`
- 示例：`smart-garden.md`、`weather-station.md`

### Front Matter 属性

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `title` | string | ✅ | 方案标题 |
| `boardId` | reference | ✅ | 关联的开发板 ID（boards 文件名） |
| `moduleIds` | array | ✅ | 关联的模块 ID 数组 |
| `difficulty` | enum | ❌ | 难度等级，默认为 `Medium` |
| `intro` | string | ❌ | 方案简介 |
| `image` | string | ❌ | 缩略图路径 |

### `difficulty` 难度枚举值

| 值 | 说明 |
|----|------|
| `Easy` | 简单 |
| `Medium` | 中等（默认） |
| `Hard` | 困难 |

### `boardId` 格式
直接使用 boards 文件名（不含扩展名）：
- 示例：`esp32-c3`、`esp32-s3`、`arduino-uno`

### `moduleIds` 格式
数组格式，每个元素为 `分类/文件名`：
- 示例：`["sensor/dht11", "display/ssd1306"]`

### 完整示例

```markdown
---
title: "智能花园监控系统"
boardId: "esp32-c3"
moduleIds:
  - "sensor/dht11"
  - "display/ssd1306"
difficulty: "Medium"
intro: "结合传感器与 OLED 屏幕，打造一个能实时显示植物生长环境的智能设备。"
---

## 项目简介

在这个项目中，我们将读取 DHT11 的温湿度数据，并在 SSD1306 OLED 屏幕上显示出来。

## 硬件清单

| 组件 | 数量 | 说明 |
|------|------|------|
| ESP32-C3 开发板 | 1 | 主控板 |
| DHT11 温湿度传感器 | 1 | 传感器 |
| SSD1306 OLED 屏幕 | 1 | 显示器 |

## 接线说明

### DHT11 接线

| DHT11 引脚 | ESP32-C3 引脚 |
|-----------|--------------|
| VCC | 3.3V |
| DATA | GPIO 4 |
| GND | GND |

### SSD1306 接线（I2C）

| SSD1306 引脚 | ESP32-C3 引脚 |
|------------|--------------|
| VCC | 3.3V |
| GND | GND |
| SDA | GPIO 6 |
| SCL | GPIO 7 |

## 功能说明

1. 每隔 2 秒读取一次温湿度数据
2. 数据实时显示在 OLED 屏幕上
3. 超出预设范围时显示警告信息

## 代码示例

```cpp
#include <DHT.h>
#include <Wire.h>
#include <Adafruit_GFX.h>
#include <Adafruit_SSD1306.h>

// DHT11 配置
#define DHTPIN 4
#define DHTTYPE DHT11

// OLED 配置
#define SCREEN_WIDTH 128
#define SCREEN_HEIGHT 64
#define OLED_RESET -1
Adafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, OLED_RESET);

DHT dht(DHTPIN, DHTTYPE);

void setup() {
  Serial.begin(115200);

  // 初始化 DHT11
  dht.begin();

  // 初始化 OLED
  if(!display.begin(SSD1306_SWITCHCAPVCC, 0x3C)) {
    Serial.println(F("SSD1306 allocation failed"));
    for(;;);
  }
  display.clearDisplay();
  display.display();
}

void loop() {
  float h = dht.readHumidity();
  float t = dht.readTemperature();

  // OLED 显示
  display.clearDisplay();
  display.setTextSize(1);
  display.setTextColor(WHITE);
  display.setCursor(0,0);
  display.println("Smart Garden");
  display.println("----------------");
  display.print("Temp: ");
  display.print(t);
  display.println(" C");
  display.print("Humidity: ");
  display.print(h);
  display.println(" %");
  display.display();

  delay(2000);
}
```

## 扩展功能

- 添加 WiFi 连接，实现远程监控
- 增加土壤湿度传感器
- 添加自动浇水功能
```

### 添加步骤

1. 确认目标开发板和所有模块已在对应集合中定义
2. 在 `src/content/solutions/zh-cn/` 或 `en/` 创建 Markdown 文件
3. 填写 Front Matter，确保 `boardId` 和 `moduleIds` 正确
4. 设置 `difficulty` 难度等级
5. 撰写完整的方案内容
6. 保存文件

---

## 博客文章 (Articles)

### 用途
发布各类技术文章、教程、观点等内容。

### 文件位置
```
src/content/articles/
├── zh-cn/
│   └── ace-step-mac-deploy.md
└── en/
    └── ace-step-mac-deploy.md
```

### 文件命名规范
- 按语言分目录存储
- 使用小写字母和连字符
- 建议格式：`简短描述.md`
- 示例：`ace-step-mac-deploy.md`、`python-async-tutorial.md`

### Front Matter 属性

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `title` | string | ✅ | 文章标题 |
| `domain` | enum | ✅ | 领域分类（决定物理路由） |
| `date` | date | ✅ | 发布日期 |
| `platforms` | array | ❌ | 平台数组（可多选） |
| `format` | enum | ❌ | 文章体裁，默认为 `tutorial` |
| `intro` | string | ❌ | 文章简介 |
| `image` | string | ❌ | 缩略图路径 |
| `tags` | array | ❌ | 标签数组 |
| `relatedBoards` | array | ❌ | 关联的开发板数组 |

### 多维度分类系统

#### 维度 1：领域（Domain）- 决定物理路由
| 值 | 说明 | URL 路径 |
|----|------|----------|
| `ai` | 人工智能相关 | `/[lang]/articles/ai/` |
| `software` | 软件开发 | `/[lang]/articles/software/` |
| `hardware` | 硬件开发 | `/[lang]/articles/hardware/` |
| `devops` | 运维部署 | `/[lang]/articles/devops/` |
| `life` | 生活分享 | `/[lang]/articles/life/` |

#### 维度 2：平台（Platforms）- 可多选
| 值 | 说明 |
|----|------|
| `mac` | macOS 平台 |
| `windows` | Windows 平台 |
| `linux` | Linux 平台 |
| `web` | Web 应用 |
| `cross-platform` | 跨平台 |

#### 维度 3：体裁（Format）- 决定阅读预期
| 值 | 说明 |
|----|------|
| `tutorial` | 教程（默认） |
| `prompt-list` | 提示词列表 |
| `opinion` | 观点文章 |
| `cheatsheet` | 速查表 |
| `news` | 新闻动态 |

#### 维度 4：硬件关联（Related Boards）- 打通软硬件生态
可关联到 boards 集合中的开发板，实现文章与硬件内容的生态打通。

### 完整示例

```markdown
---
title: "【保姆级】零成本！教你在 Mac 上本地部署 ACE-Step 1.5，一键生成 AI 音乐"
domain: ai
platforms: ["mac"]
format: "tutorial"
date: 2026-02-23
intro: "在自己的 Mac 上完全免费、离线运行 AI 音乐生成模型 ACE-Step 1.5，Apple 芯片加速，全程只需几行命令。"
tags: ["AI", "Mac", "音乐生成", "ACE-Step", "本地部署"]
---

既然大家对本地跑 AI 这么感兴趣，今天分享个好玩的项目——**ACE-Step 1.5**。

简单说，就是能在你自己的 Mac 上生成音乐，完全免费，不用联网也能跑，而且在这个项目上，Apple 芯片的性能优化做得挺不错。

不用担心很难，全程只需要敲几行代码。跟着我的步骤来，几分钟就能搞定！

## 准备工作

打开你的终端（Terminal），我们开始吧。

### 1. 把"窝"搭好

首先，找个你顺眼的地方存放项目。

```bash
cd Projects
cd Python
```

### 2. 拉取代码 & 安装依赖

直接把项目从 GitHub 上克隆下来，然后我们用 `uv` 来快速安装依赖。

```bash
git clone https://github.com/ACE-Step/ACE-Step-1.5.git
cd ACE-Step-1.5
uv sync
```

### 3. 检查一下 Mac 的"马力"

装好后，先别急着跑，确认一下你的 Apple 芯片（MPS加速）是不是被正确识别了。

```bash
uv run python -c "import torch; print(f'MPS check: {torch.backends.mps.is_available()}')"
```

只要终端返回了 **`MPS check: True`**，就说明一切正常！

## 启动与运行

### 4. 启动服务

```bash
uv run acestep
```

### 5. 进网页开玩

终端那边跑起来后，打开浏览器，输入：`127.0.0.1:7860`

### 6. 生成你的第一首 AI 音乐

1. 切到 **「Simple」** 模式
2. 输入提示词，不知道写啥就填个 `easy example`
3. 点 **「Create Sample」**
4. 直接点 **「Generate Music」**

搞定！稍等一会儿，进度条走完，就能听到本地显卡"算"出来的音乐了 🎵
```

### 各种体裁示例

#### 教程 (Tutorial)
```markdown
---
title: "Python 异步编程入门"
domain: software
format: tutorial
platforms: ["cross-platform"]
date: 2026-02-26
intro: "从零开始学习 Python async/await，掌握异步编程的核心概念。"
tags: ["Python", "异步", "async", "await"]
---

# 异步编程基础

## 什么是异步编程？

异步编程是一种...
```

#### 提示词列表 (Prompt-list)
```markdown
---
title: "AI 绘画提示词大全"
domain: ai
format: prompt-list
platforms: ["web"]
date: 2026-02-26
intro: "收集了 50+ 个高质量的 AI 绘画提示词，涵盖多种风格和主题。"
tags: ["AI", "提示词", "Midjourney", "Stable Diffusion"]
---

## 风格类

- **赛博朋克风格**：`futuristic city at night, neon lights, cyberpunk style, highly detailed`
- **水彩画风格**：`watercolor painting, soft edges, pastel colors, dreamy atmosphere`
```

#### 观点文章 (Opinion)
```markdown
---
title: "为什么我选择放弃框架，回归原生开发"
domain: software
format: opinion
platforms: ["cross-platform"]
date: 2026-02-26
intro: "使用多年框架后，我决定回归原生开发，这是我的思考过程。"
tags: ["观点", "原生开发", "框架"]
---

# 前言

在这个框架层出不穷的时代，选择回归原生似乎是一种"倒退"...

## 我的观点

我认为...
```

#### 速查表 (Cheatsheet)
```markdown
---
title: "Git 命令速查表"
domain: software
format: cheatsheet
platforms: ["cross-platform"]
date: 2026-02-26
intro: "常用 Git 命令的快速参考，一页纸掌握日常开发所需。"
tags: ["Git", "速查表", "命令"]
---

## 基础操作

| 命令 | 说明 |
|------|------|
| `git init` | 初始化仓库 |
| `git clone <url>` | 克隆仓库 |
| `git add .` | 添加所有文件 |
| `git commit -m "msg"` | 提交更改 |

## 分支操作

| 命令 | 说明 |
|------|------|
| `git branch` | 查看分支 |
| `git checkout -b <name>` | 创建并切换分支 |
| `git merge <branch>` | 合并分支 |
```

#### 新闻动态 (News)
```markdown
---
title: "Astro 5.0 正式发布，带来重大更新"
domain: software
format: news
platforms: ["web"]
date: 2026-02-26
intro: "Astro 5.0 带来了全新的视图层和性能优化。"
tags: ["Astro", "新闻", "发布"]
---

# Astro 5.0 发布

Astro 团队今天正式发布了 5.0 版本...

## 主要更新

1. 全新的视图层架构
2. 性能提升 40%
3. 改进的开发体验
```

### 添加步骤

1. 确定文章的领域（`domain`）和体裁（`format`）
2. 在 `src/content/articles/zh-cn/` 或 `en/` 创建 Markdown 文件
3. 填写 Front Matter，确保必需字段完整
4. 撰写文章内容
5. 保存文件，系统会自动生成对应路由

---

## 多语言支持

### 支持的语言

当前支持：
- **zh-cn** - 简体中文
- **en** - 英文

### 多语言实现方式

通过目录前缀实现：

```
src/content/[collection]/
├── zh-cn/
│   └── 文件名.md
└── en/
    └── 文件名.md
```

### URL 结构

多语言通过路由前缀实现：
- 中文：`/zh-cn/articles/ai/slug`
- 英文：`/en/articles/ai/slug`

### 添加多语言内容

对于同一内容的多个语言版本：

1. 使用相同的文件名
2. 分别放在 `zh-cn/` 和 `en/` 目录中
3. 分别撰写不同语言的内容

示例：
```
src/content/articles/
├── zh-cn/ace-step-mac-deploy.md
└── en/ace-step-mac-deploy.md
```

---

## 关联关系说明

### 内容集合关联图

```
boards (开发板)
    ├── 被 experiments 引用 (1对1)
    ├── 被 solutions 引用 (1对多)
    └── 被 articles 引用 (0对多)

modules (模块)
    ├── 被 moduleDocs 描述 (1对1)
    ├── 被 experiments 引用 (1对1)
    └── 被 solutions 引用 (1对多)
```

### Reference 机制

Astro Content 的 `reference()` 函数用于建立集合间的关联：

```typescript
// 关联 boards
boardId: reference('boards')

// 关联 modules
moduleId: reference('modules')
moduleIds: z.array(reference('modules'))

// 关联 boards 到 articles
relatedBoards: z.array(reference('boards')).optional()
```

### 关联 ID 格式

| 引用类型 | 格式 | 示例 |
|----------|------|------|
| boards | 文件名（不含扩展名） | `esp32-c3` |
| modules | `分类/文件名` | `sensor/dht11` |

### 数据完整性

使用 `reference()` 可以：
- 确保引用的目标存在
- 在构建时验证数据完整性
- 提供类型安全的关联访问

---

## 常见问题

### Q1: 文章添加后没有显示？
A: 检查以下几点：
1. 文件是否保存
2. Front Matter 格式是否正确
3. 必填字段是否完整
4. 运行 `npm run build` 重新构建

### Q2: 如何添加新的平台？
A: 在 `src/content/config.ts` 中修改 `platforms` 枚举：

```typescript
platforms: z.array(z.enum([
  'mac', 'windows', 'linux', 'web', 'cross-platform', 'new-platform'
])).optional()
```

### Q3: 如何添加新的领域？
A: 在 `src/content/config.ts` 中修改 `domain` 枚举：

```typescript
domain: z.enum([
  'ai', 'software', 'hardware', 'devops', 'life', 'new-domain'
])
```

注意：添加新领域后需要创建对应的页面路由文件。

### Q4: 图片应该放在哪里？
A: 图片应放在 `public/` 目录下，然后在文件中使用相对路径引用：

```yaml
image: "images/article-cover.jpg"
```

### Q5: 如何预览内容？
A: 运行开发服务器：

```bash
npm run dev
```

然后访问对应的 URL。

### Q6: 文件命名有什么限制？
A:
- 使用小写字母
- 使用连字符分隔单词
- 避免使用特殊字符
- 避免使用中文（文件名）

### Q7: 如何删除内容？
A:
1. 删除对应的文件即可
2. 系统会自动更新，无需额外操作

### Q8: 如何批量添加内容？
A:
1. 准备好所有文件
2. 确保格式正确
3. 一次性放置到对应目录
4. 重新构建项目

---

## 附录：快速参考

### Boards 必填字段
```json
{
  "brand": "必填",
  "model": "必填"
}
```

### Modules 必填字段
```json
{
  "category": "必填 (sensor/display/actuator/communication/power/other)",
  "model": "必填"
}
```

### Experiments 必填字段
```yaml
---
title: "必填"
boardId: "必填 (boards文件名)"
moduleId: "必填 (分类/文件名)"
---
```

### Solutions 必填字段
```yaml
---
title: "必填"
boardId: "必填 (boards文件名)"
moduleIds: ["必填 - 模块ID数组"]
---
```

### Articles 必填字段
```yaml
---
title: "必填"
domain: "必填 (ai/software/hardware/devops/life)"
date: 必填 (日期)
---
```

---

*最后更新日期：2026-02-26*
